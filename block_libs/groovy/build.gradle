apply plugin: 'groovy'
apply from: rootProject.ext.configs.maven_push

dependencies {
    //gradle sdk
    compile gradleApi()
    //groovy sdk
    compile localGroovy()
}

repositories {
    mavenCentral()
}
